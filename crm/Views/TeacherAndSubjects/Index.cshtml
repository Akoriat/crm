@model IEnumerable<crm.Models.TeacherAndSubject>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Teacher)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Subject)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Teacher.TeacherFirstName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Subject.SubjectName)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.TeacherAndSubjectId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.TeacherAndSubjectId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.TeacherAndSubjectId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
